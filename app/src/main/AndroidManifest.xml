<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhangchuang.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".ui.notifications.UserinfoActivity"></activity>
        <activity android:name=".ui.start.EditConfigActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.TestActivity" />
        <activity android:name=".ui.start.FourActivity" />
        <activity android:name=".ui.start.ThreeActivity" />
        <activity android:name=".ui.start.TwoActivity" />
        <activity android:name=".ui.start.OneActivity" />
        <activity android:name=".ui.start.GuideActivity" />
        <activity
            android:name=".ui.start.StartActivity"
            android:enabled="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
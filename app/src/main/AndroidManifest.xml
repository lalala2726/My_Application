<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhangchuang.demo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.live.FeedBackActivity" />
        <activity android:name=".ui.function.OrderActivity" />
        <activity android:name=".ui.function.NewsInfoActivity" />
        <activity android:name=".ui.notifications.FeedbackActivity" />
        <activity android:name=".ui.function.StopCarActivity" />
        <activity android:name=".ui.notifications.UpdatePasswordActivity" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.notifications.UserinfoActivity" />
        <activity android:name=".ui.start.EditConfigActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.TestActivity" />
        <activity android:name=".ui.start.FourActivity" />
        <activity android:name=".ui.start.ThreeActivity" />
        <activity android:name=".ui.start.TwoActivity" />
        <activity android:name=".ui.start.OneActivity" />
        <activity android:name=".ui.start.GuideActivity" />
        <activity
            android:name=".ui.start.StartActivity"
            android:enabled="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>